import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF8AFF8A);
  static const secondary = Color(0xFF0A0A20);
  static const tertiary = Color(0xFF050510);
  static const neutral = Color(0xFF72796E);

  // Dark theme
  static const darkBackground = Color(0xFF050510);
  static const darkSurface = Color(0xFF10180F);
  static const darkSurfaceSoft = Color(0xFF162014);
  static const darkCard = Color(0xFF121A11);

  // Text
  static const textPrimary = Color(0xFFEAF5E7);
  static const textSecondary = Color(0xFF9FA99B);
  static const textMuted = Color(0xFF72796E);

  // States
  static const success = Color(0xFF8AFF8A);
  static const warning = Color(0xFFFFD166);
  static const error = Color(0xFFFF6B6B);

  // Light theme
  static const lightBackground = Color(0xFFF5F8F3);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFEAF0E7);
  static const lightTextPrimary = Color(0xFF10180F);
  static const lightTextSecondary = Color(0xFF4F5A4A);
}
